const assert = require('assert');
const plexAPI = require('./index');

describe('', function() {

  /**
   * Test to implement:  need to test the function 'extractVideoUrls' to ensure that the behaviour adapts when:
    - no videos are in the WL list
    - only 1 video in the WL list
    - 2+ videos in the WL list
   */
  describe('', function() {
    it('test pending implementation');
  });
});